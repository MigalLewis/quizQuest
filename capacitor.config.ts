import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'biz.pacifish',
  appName: 'quizQuest',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
